% GENERATED by analysis/emit_tex.py — do not edit by hand.
% Every number is recomputed from per-item rows; see the analysis scripts.

\begin{table}[t]
\centering\small
\setlength{\tabcolsep}{5pt}\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}p{4.2cm} c c c c c@{}}
\toprule
\textbf{Condition} & \textbf{Flaws found} & \textbf{Rate} & \textbf{Calls}
 & \textbf{$\Delta$} & \textbf{Paired 95\%} \\
 & & & per clip & flaws & \textbf{interval} \\
\midrule
Full plan-based critic & 27/48 & 56.2\% & 10.9 & --- & --- \\
\midrule
No shared perception & 27/48 & 56.2\% & 12.1 & +0 & $[+0, +0]$ \\
\bottomrule
\end{tabular}
\caption{Mechanism ablation on 26 clips of the frozen set carrying 48
human flaws. Each row is a \emph{separate live run} of the identical plan with one mechanism
disabled, scored against the same labels and paired clip-by-clip against the full condition
above; the interval is a paired bootstrap over clips. This is deliberately not a subtraction
from a finished run, which would assume every finding the mechanism produced becomes a miss
rather than being recovered another way. Removing call deduplication found exactly the same flaws --- the paired interval is zero --- while spending +1.19 calls per clip more. Sharing is therefore a cost saving on this data and not a source of recall. The saving is concentrated rather than spread: only 5 of 26 clips had any duplicate perception query to collapse, and on those the ablation spent between two and fourteen extra calls. A positive control confirms the mechanism really was disabled rather than the switch having no effect: on every clip where the full condition deduplicated, the ablation demonstrably spent more calls, and no ablation row reports a saving.}
\label{tab:ablation}
\end{table}
