% =====================================================================
%  Adobe Research Technical Report -- Toward physics-faithful video generation:
%  a depth-guided generator, a human-annotated flaw benchmark, and an
%  auditable multi-tool critic.
%  Compile: pdflatex main -> bibtex main -> pdflatex main x2
%
%  The document uses the adobe_research class and assets from the Arceus
%  technical-report project (69fad61ed0f1a0808bc0f18f).
%
% =====================================================================
\documentclass{adobe_research}
\usepackage{diffphy-support}

% Canonical method-name symbol. Use this command for every typeset mention.
\DeclareRobustCommand{\PhyReAct}{{\fontfamily{lmtt}\selectfont VeriPhy}}
\pdfstringdefDisableCommands{%
  \def\PhyReAct{VeriPhy}%
}

%\title{Toward Physics-Faithful Video Generation:\\
%\large A Depth-Guided Generator, a Human-Annotated Flaw Benchmark,
%and an Auditable Multi-Tool Critic}
\title{\PhyReAct: Agentic Physical Reasoning for World Model Evaluation and Refinement}
\author[1\S]{Wenzhuo Xu}
\author[2]{Yuchen Zhu}
\author[3\ddagger]{Chongjian Ge}
\author[3\ddagger]{Jing Shi}
\author[3\ddagger]{Jason Kuen}
\author[3\S\dagger]{Jiuxiang Gu}

\affiliation[1]{Carnegie Mellon University}
\affiliation[2]{Georgia Institute of Technology}
\affiliation[3]{Adobe Research}


% \contribution[*]{Equal Contribution}
\contribution[\S]{Core Contribution}
\contribution[\dagger]{Project Lead}

\date{July 2026}
\hypersetup{
  pdftitle={\PhyReAct: Agentic Physical Reasoning for World Model Evaluation and Refinement},
  pdfauthor={Wenzhuo Xu, Yuchen Zhu, Chongjian Ge, Jing Shi, Jason Kuen, Jiuxiang Gu},
  pdfsubject={Agentic physical reasoning for video evaluation and refinement}
}

\abstract{
Visual fluency in generated video does not imply physical reliability, and a scalar quality
score names neither the obligation a clip violates nor the moment it fails. We present
\PhyReAct{}, an auditable physical-verification system in which a text-only planner compiles
the prompt into typed physical obligations and a statically validated execution plan before
any frame is observed. During execution, observations gate and scope only declared calls to
frozen low-level experts---segmentation and tracking, counting, eleven typed physical
measurements over the resulting tracks, depth, OCR, and audio-event detection. Each action
returns a provenance-carrying evidence record whose payload, when usable, is either a typed
measurement or an explicitly tagged learned state. Typed resolvers and fixed composition map usable records to
a three-valued state (\supported{},
\contradicted{}, or \unknownv{}, surfaced as \emph{plausible},
\emph{implausible}, or \abstain{}) with full provenance, so that every verdict is
traceable to the evidence that produced it. We anchor evaluation in a
1{,}500-clip corpus of human-annotated flaw records that localize real generation
failures in prompt reference, space, and time. On a $149$-clip core carrying $304$
such records, \PhyReAct{} accounts for $228$, against $164$ for a published
question-decomposition evaluator given the same clips and the same claims. Recall alone does
not separate it from prompting the same backbone monolithically, which reaches $222$; what
separates them is that each decision retains its evidence record and provenance, making the
traces auditable one verdict at a time and usable as the interface through which a critic
verdict could be written back into generation. The core is a development set, so these
figures characterize the system rather than its generalization.
}

% convenience macros
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\supported}{\textsf{supported}}
\newcommand{\contradicted}{\textsf{contradicted}}
\newcommand{\unknownv}{\textsf{unknown}}
\newcommand{\abstain}{\textsf{abstain}}
\newcommand{\TODO}[1]{\textcolor{adobered}{\textbf{[TODO: #1]}}}

% figures are built by figs/make_fig_generation.py (and siblings)
\graphicspath{{figs/}}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage{float}
\usepackage{algorithm}
\usepackage{algpseudocode}

% ---- float placement -------------------------------------------------
% Every figure must appear before the bibliography. Two things make that
% hold. (1) Size: no figure PDF is taller than the text block less its own
% caption, so none of them is float-page-only and none can dam the queue
% behind it. (2) A barrier at every \section, so an unplaced float cannot
% cascade out of the section that discusses it and pile up at the end of
% the document. The fractions below simply let a page carry more float and
% less text than the article-class defaults, which are tuned for far fewer
% figures than this report has.
\pretocmd{\section}{\FloatBarrier}{}{}
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.75}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.62}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}

% ---- compaction: tighten vertical whitespace without changing the body
% line spacing or font size. Display-math skips and float/caption gaps are
% the biggest space sinks in this equation- and figure-heavy report, so we
% trim those; list spacing is already set tight in diffphy-support.sty.
\setlength{\abovedisplayskip}{4pt plus 2pt minus 2pt}
\setlength{\belowdisplayskip}{4pt plus 2pt minus 2pt}
\setlength{\abovedisplayshortskip}{2pt plus 1pt}
\setlength{\belowdisplayshortskip}{3pt plus 1pt minus 1pt}
\setlength{\floatsep}{7pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{9pt plus 2pt minus 3pt}
\setlength{\intextsep}{7pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{9pt plus 2pt minus 3pt}
\setlength{\dblfloatsep}{7pt plus 2pt minus 2pt}

\begin{document}
\maketitle

% ---- code-release footnote on the first page (no marker) ----
\begingroup
\renewcommand\thefootnote{}\footnotetext{Code and project page: \url{https://phyreact.ai}.}%
\addtocounter{footnote}{0}%
\endgroup

% ---- body: one file per section under sections/ ----
\input{sections/01-introduction}
\input{sections/02-related-work}
\input{sections/03-physics-guided-simulation-pipeline}
\input{sections/04-phyreact-critic}
\input{sections/06-experiments}
\input{sections/09-limitations}
\input{sections/10-conclusion}

% ---- appendix: the state-optimization framework and roadmap are
%      supporting material, moved out of the main text ----
\input{sections/99-appendix}
\input{sections/07-framework-learning-as-state-optimization}
\input{sections/08-an-executable-roadmap}

\bibliographystyle{assets/plainnat}
\bibliography{references}

\end{document}

